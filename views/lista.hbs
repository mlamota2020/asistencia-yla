<div style="display: flex;
    flex-direction: row;
    flex-wrap: nowrap;
    justify-content: space-between;
    align-items: center;">
    <div><h1 style="margin-right:65vw;">Lista de estudiantes</h1></div>
   <div><a href="/">Volver</a></div>
   
</div>

{{#if success}}
  <div style="color: green; font-weight: bold;" id="messages">{{success}}</div>
{{/if}}
{{#if error}}
  <div style="color: red; font-weight: bold;" id="messages">{{error}}</div>
{{/if}}

<table cellpadding="5" cellspacing="10" border="1"  style=" text-align: center;width: 85%;" id="listita">
  <thead style="background-color: #f0f0f0;">
    <tr>
      <th>Nombre</th>
      <th>Curso</th>
      {{#each weekDates}}
        <th>{{this}}</th>
      {{/each}}
    </tr>
  </thead>
  <tbody style="font-size:small;">
    {{#each estudiantes}}
      <tr>
        <td style="font-weight:bold;" id="nombres">{{this.nombre}} {{this.apellido}}</td>
        <td>{{this.curso}}</td>
        {{#each this.displayAttendance}}
          <td style="
            {{#if (eq this 'Presente')}}background-color: #15241c;{{/if}}
            {{#if (eq this 'Atrasado')}}background-color: #3b3600;border-radius:30px;padding:5px;{{/if}}
            {{#if (eq this 'No ha Asistido')}}background-color: #300000;{{/if}};border-radius:30px;">
            {{this}}
          </td>
        {{/each}}
        
      </tr>
    {{/each}}
  </tbody>
</table>

